# JavaScript calculator

### 1. input 초기화



### 2. click

- 연산자(+ - * / =) 클릭


- 숫자(1~9 , .) 클릭


- clear 클릭



### 3. 변수

정답이라는 변수(그 안에 숫자 변수 2개, 연산자 변수 1개)

화면에 출력 : 첫번쨰변수 + 연산자변수


첫번쨰 변수 : 1
연산자 변수 : +
두번쨰 변수 : 2

// 연산 전에 숫자와 연산자 입력 ////////////////////////////////////////////////////////////////////////////////////////////////////

// 클릭한 숫자, 연산자를 배열에 넣어주기.

- 1. 빈배열을 만들어 준다. 전역변수로 input 만듦 

- 2. 숫자와 연산자를 클릭 했을 때 배열에 넣어야함.

- 3. input에 첫 번째로 숫자가 없다면 연산자를 사용 못하게 해야함 if문
     연산자는 중복으로 사용 못하게 해야함 if문
        - 버튼 클릭 시 연산자 추가
        - 배열의 마지막 요소가 + - * / 라면 return

- 4. 숫자를 연속해서 누를때 배열 하나의 요소에 추가. // 아직 해결 못함..

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////




// 연산 시작 //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// forEach문으로 배열을 돌면서 *  / 우선 찾기. 
- 1. * / 찾으려면 

// * / 에 있는 인덱스 번호에 +1 -1 한 값을 가져와서 계산하기

// 그걸 다시 배열에 푸쉬.

// 반복
///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////




### 4. 유의점

// input에 첫 번째로 숫자가 없다면 연산자를 사용 못하게 해야함 if문
// 연산자는 중복으로 사용 못하게 해야함 if문
// 숫자를 중복으로 누르면 배열 하나의 요소에 넣어야함.



### 5. 미완성 목록

1. 숫자를 중복으로 눌렀을 때 배열 하나의 요소에 합쳐야함. 고민
2. 소수점. 고민
3. clear시 배열의 모든 요소 삭제. input = []
4. 초기 화면에 0 으로 보이게. if문 사용해서 배열에 값이 들어오면 "" 로 초기화.





### 6. 숫자 중복으로 입력했을 때

1. 전역변수 2개를 생성
        - 이전에 누른 값
        - 현재 누른 값

2. 숫자랑 연산자를 클릭하면 
        - 1. 현재 누른 값에 넣어줌
        - 2. 다음 어떠한 버튼을 누르면 현재 누른 값에 있는걸 이전에 누른 값으로 바꿔주고, 현재 누른 값에 현재 누른 값을 할당
        - 3. 이전에 누른 값에 대한 조건 
                - 숫자일 때 계속 추가
                - 연산자일 때 push? 
                


연산자 입장
- currentValue에 연산자가 아닌 다른게 있다면 currentValue를 beforValue로 옮겨주고 currentValue에 연산자를 할당. 그리고 여기서 beforValue를 푸쉬
- currentValue에 연산자가 있다면? 리턴